# CONTEXT.md — 放置游戏引擎框架 + 《问道长生》（项目代号 SmallRpg）

> 本文件是项目的共享认知载体：领域词汇 + 已定决策。所有后续 spec/实现以此为据。
> 维护规则：词汇变更须更新本文件并说明日期与理由。

## 一句话定位

**放置游戏引擎框架**（2026-09-05 转向，ADR-017）：以 engine（机制核心）+ content 协议（schema/类型/校验）+ 壳层义务为产品本体，供开发者以"内容包 + 壳"组装自己的放置游戏；验收 = 第二款不同题材游戏只写内容包+壳跑通。游戏《问道长生》（单机放置修仙，类梅尔沃：挂机修炼六艺 → 炼丹炼器 → 斗法夺宝 → 兵解转生长线循环；桌面 Steam 优先，移动端随后）降格为框架的第一款狗粮/参考实现，继续并行开发。生成器层（脚手架+AI 内容管线）为后置第二层，框架第一期不实施。

## 硬原则（不可违反）

1. **引擎与内容完全隔离**：引擎（engine）零内容感知——技能、物品、敌人、配方、文案词库、转生/秘境等玩法参数全部由 content 包定义；引擎只提供机制与事件，不知道任何具体内容的存在。新内容 = 新 JSON，不改引擎代码。
2. **依赖零全局污染**：所有环境依赖安装在项目内（node_modules 本地），不全局安装任何工具；玩家侧产物双击即玩，零命令行。
3. **内容包可校验**：每类 content 都有 JSON Schema，启动时与编辑器导入前强校验，AI 批量生成的内容必须过 schema 才能进包。

## 领域词汇

| 词汇 | 含义 |
|---|---|
| 修为 / 层 | 技能经验 / 技能等级（1~99） |
| 境界 | 斗法修为映射的称号（练气→大乘） |
| 灵石 | 通用货币 |
| 乾坤袋 | 背包 |
| 异宝 | 妖物专属掉落装备（不可炼制，随机稀有度+词条） |
| 稀有度 | 装备品质档位；档名/倍率/词条数/掷点概率等具体值由 content 包定义（ADR-016），引擎只保留掷点机制与缺档安全回退 |
| 器胚 | 装备底材（模板）：槽位 + 掉落层数段(floorRange) + 纹阶天花板(tierRange) + 偏好标签(preferredTags) + 胚纹；分层掉不同器胚，否决 itemLevel 缩放 |
| 铭纹 | 装备词缀（模板）：三阶数值表(tiers) + feature 机制型特色铭纹（condition+primitive 表达，引擎零新增）；自带 tags 供标签加权抽取 |
| 纹阶 | 铭纹阶位 T1~T3，实例化时按器胚 tierRange 掷出；口诀：稀有度是因（词条数），纹阶是果（质量） |
| 重铸铭纹 | 洗练：消耗材料重随单条铭纹数值，天花板由器胚 tierRange 数据锁死 |
| 熔炼 | 分解装备得器屑 |
| 器屑 | 熔炼产物材料，重铸铭纹等装备循环的消耗方（打通炼器经济） |
| 胚纹 | 器胚固有词条（固定非随机，实例化时直接附加） |
| 系别 | 灵系：金木水火土风雷 + 凡击；每系一个可观测机制签名，拒绝纯数值系（ADR-012） |
| 修饰符 | 属性调整最小单元 `{stat, zone, value, condition?}`；一切产出方（铭纹/丹药/宗门/天赋）走同一聚合管线 flat→加法%→乘法区，禁绕管直改（ADR-011，#13） |
| 凡击 | 无系别攻击 |
| 兵解重修 | 转生：重置修为与资源，保留部分资产，结算道韵 |
| 道韵 | 转生货币 → 永久天赋与内容解锁 |
| 秘境 | 分层爬塔战斗场景（待建） |
| content 包 | 一组符合 schema 的 JSON 内容文件，可整体导入/导出 |
| 引擎 | 无内容感知的机制核心：状态、挂机循环、战斗解算、事件流 |
| 框架层 | 引擎的产品形态（ADR-017）：engine 机制 + content 协议（schema/types/validate）+ 壳层义务（snapshot 零公式、系统文案归 content），开发者面向它组装游戏 |
| 题材包 | 框架层之上的一份具体题材内容（现：《问道长生》修仙包）。框架不含缺省题材包；content 包内 `src/schema/`（通用协议）与 `src/packs/`（题材）分居（ADR-017） |
| 生成器 | 后置第二层：脚手架模板 + AI 内容管线，批量产出成品游戏；框架 API 稳定前不实施（ADR-017） |
| 第二款游戏 | 框架的验收载体：西方魔幻题材放置游戏，零引擎改动、只写内容包+壳跑通即验收（ADR-017） |

## 已定决策（ADR 简表）

| # | 决策 | 理由 | 日期 |
|---|---|---|---|
| 001 | 桌面壳用 **Electron**（steamworks.js），Android 后续用 **Capacitor** 包同一套 web 代码 | steamworks.js 活跃成熟（已核实）；游戏逻辑是纯 web 代码零改动；Melvor 同款路线 | 2026-09-02 |
| 002 | Steam 第一版只做 **成就 + 云存档** | 放置游戏刚需；排行榜/创意工坊延后，编辑器成熟后工坊联动 | 2026-09-02 |
| 003 | **核心直接重写**，现有 data.js/game.js 仅作设计参考不作保留 | 用户决策；现版本按"可能有 bug"对待 | 2026-09-02 |
| 004 | 数据格式 **content/*.json + schemas/*.json + 启动校验** | 编辑器表单可由 schema 生成；AI 生成内容可机器校验；SexyMUD 管线先例 | 2026-09-02 |
| 005 | 编辑器为**独立本地 web 工具** | 与游戏解耦；产出 content 包可上传创意工坊 | 2026-09-02 |
| 006 | LLM 接入为 **OpenAI 兼容接口 + 自定义 baseURL/key** | 一套配置通吃 OpenAI/DeepSeek/硅基流动等 | 2026-09-02 |
| 007 | 内容扩充优先级：**转生 → 秘境分层 → Boss 机制 → 成就统计**；新技能/装备强化次之；宗门最后 | 放置游戏长线骨架；新内容优先设计为现有系统的下游消耗方 | 2026-09-02 |
| 008 | 工程：**TypeScript + vite + workspace**（packages/engine、content、editor、app-desktop），UI 全新设计（仙侠主题自由发挥，不受旧版约束），旧存档不迁移 | 重写时机转 TS 收益最大；依赖全项目内（npm workspaces，不用全局包管理器） | 2026-09-02 |
| 009 | Steamworks 账号状态 C：开发期用 SDK 模拟模式，账号/AppID 由用户并行办理 | $100 上架费与资料只有用户能办 | 2026-09-02 |
| 010 | 内容包定版（#2）：节名 `combatText`/`gearDrops`；耗时字段统一**毫秒**；丹药效果数据化（回气丹补 `heal.percent=0.3`，原为引擎硬编码）；引擎兜底约定=**未注册招式一律回退拳脚**（moves.fist + verbs.fist 由校验保证恒在） | 旧硬编码数值随重写数据化；引擎对缺内容零崩溃；schema 管形态、语义校验管跨引用 | 2026-09-02 |
| 011 | **修饰符聚合统一管线**：flat→加法%→乘法区一条管线；铭纹/心法/丹药 buff/转生天赋全部产出修饰符走同管线，禁止绕管直改属性；先于战斗系统实现 | 旧版 equipBonus 与 buffMult 两路分算，未来数值来源会持续分叉；转生天赋将来直接复用 | 2026-09-02 |
| 012 | **系别=结构签名**：每系占一个可观测机制维度（雷·霆爆=暴击/金·破防/水·滞缓/风·迅疾/火·燃爆/木·疫毒/土·守护），拒绝纯数值系；验收=只读战斗日志能说出流派，不过就砍系（宁要 4 个真系不要 7 个假系） | 纯系数玩家无感（SexyMUD v2 教训：5/7 系等于不存在却占设计成本）；数值等价≠体验等价 | 2026-09-02 |
| 013 | **确定性纪律**：三禁（Math.random/Date.now/隐式定时器），随机走注入的种子化 RNG；DoT/状态用**观察时补偿结算**（纯函数 f(startTick, now)，被观察时补齐欠账写回 applied）；资源设计保持离线 O(1) 可解；存档未显式写入字段不落盘 | 挂机游戏地基；离线结算退化为逐步回放是灾难；替代旧版 migrateXxx 幂等修补 | 2026-09-02 |
| 014 | **宗门解耦**：已学功法是玩家资产不随门派没收（换派/偷师/叛派只是获取权限问题）；门派=功法池+缺省灵系+生产加成三件套；脉=武功池归类标签非玩家身份 | "学过的不没收"；流派由玩家玩出来不预设标签（SexyMUD ADR-0014）；宗门票前写入避免返工 | 2026-09-02 |
| 015 | **内容工程纪律**：id 一经发布不可变；`prototypeKey`/`prototypeParent` 第一天预留（加载期展平继承、环检测双保险）；tags/flags 分工（归类批量捞 vs 裸布尔）；schema 变更三处同步（schemas/+引擎类型/字段约定文档） | 变体靠原型继承不靠笛卡尔积；事后补原型继承是灾难（SexyMUD ADR-0030） | 2026-09-02 |
| 016 | **档位词表与文案归 content（隔离审计修复批 0，docs/audit/2026-09-03）**：①缺省分策——词表（稀有度/词条池）与文案 validate 强制恒在、引擎零默认（ADR-010 先例：校验恒在+机制兜底，兜底降级为非文案占位）；数值参数引擎内置基线+config 覆盖；②引擎 snapshot 暴露 locked，UI 零公式复算；③系统文案全归 content 包（texts 节 + combatText 扩节），引擎零中文字符串；④rarity def 加显式 bool 驱动 UI 特判；⑤gearDrops 稀有度权重随 #14；⑥verbStyle/EnemyKind 键域开放+存在性校验；⑦N2 修复并入批 1 | engine 持有词表/文案/参数违反铁律（审计 P0-1/P0-2/P0-3、N1-N4；SPEC:55/63）；修复路线审计 §八 批 0-4 | 2026-09-03 |
| 017 | **项目转向：从开发游戏到开发放置游戏引擎框架**（细目 docs/adr/0017-engine-pivot.md）：第一期只做框架层（生成器后置）；第一阶段服务开发者用户；游戏本体继续狗粮；monorepo 不拆；验收=第二款（西方魔幻）只写内容包+壳跑通；content 包内分目录+default 缺省题材包地位取消；机制键改名 gp→gold/pill→consumable/fist→basic（scope 中性化后置）；Element 键域开放+CombatText 槽位审查不重构；UI 自写壳路线（engine 义务=清公式复算+外壳文案归 content） | 引擎机制核心已基本通用、修仙性沉淀可枚举，框架化边际成本低；editor 空壳使创作者路线后置；未发布=唯一免费改名窗口 | 2026-09-05 |

## 内容与引擎的边界（示例）

- 引擎提供：挂机循环、回合战斗解算、经验曲线函数、稀有度 roll、转生结算框架、事件流（damage/loot/levelup…）
- 内容定义：有哪些技能与活动、敌人数值与掉落表、配方、丹药效果、稀有度档位表与词条池、战斗文案词库与系统文案（CTEXT 体系数据化，ADR-016）、转生天赋树节点、秘境层表
- 验证：schema 拒绝坏内容；引擎对"缺内容"有安全兜底（如未注册招式回退基础动作）

## 遗留参考

- 旧版战斗文案体系（借鉴 SexyMUD ADR-0011 适配修仙）与装备实例/稀有度设计在 git 历史中，重写时作为设计输入。
- SexyMUD 项目（D:\My_Projects\SexyMUD）**只读参考**，严禁修改其中任何文件。
