{
  "name": "wendao-changsheng",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/engine",
    "packages/content",
    "packages/editor",
    "packages/app-desktop"
  ],
  "scripts": {
    "setup": "git config core.hooksPath .githooks",
    "dev": "npm run dev -w @wendao/app-desktop",
    "dev:editor": "npm run dev -w @wendao/editor",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "build:desktop": "npm run build -w @wendao/app-desktop && npm run dist -w @wendao/app-desktop",
    "check": "npm run check --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
